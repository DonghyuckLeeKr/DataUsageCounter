# 돌핀 데이터 (Dolphin Data) - 변경 이력 (CHANGELOG)

모든 주요 변경 사항과 릴리즈 내역이 이 파일에 기록됩니다.

---

## [1.2.0] - 2026-08-13

### 주요 추가 기능 (New Features)
- **윈도우 부팅 시 자동 실행 지원 (`set_auto_start`)**:
  - 환경 설정에서 원클릭으로 윈도우 시작 프로그램 등록/해제 지원. PC 부팅 시 백그라운드 트레이로 자동 실행.
- **일일 데이터 폭주 방지 경고 리미터 (`checkAndNotifyDailySurge`)**:
  - 하루 동안 지정한 한도(예: 5GB)를 초과하여 데이터를 소모할 경우 즉시 윈도우 푸시 알림으로 경고.
- **일별 데이터 소비 캘린더 & 통계 리포트 (`DailyHistoryModal.jsx`)**:
  - 상단 [캘린더] 버튼으로 최근 14일간의 일별 소비 추이 막대 차트, 일평균 사용량, 최대 소비일 분석 모달 제공.
- **프로세스별 누적 데이터 사용량 추적 (`AppBreakdownCard.jsx`)**:
  - 내 PC의 프로그램들이 소모한 총 데이터 누적량을 기록하고, 누적순/속도순 정렬 및 개별 킬 스위치/초기화 기능 제공.
- **시스템 트레이 마우스 오버 실시간 사용량 툴팁 (`update_tray_tooltip`)**:
  - 윈도우 작업표시줄 트레이 아이콘에 마우스를 올리면 활성 프로필명, 사용량/한도(GB), 소진율(%), 실시간 다운/업로드 속도가 즉시 툴팁으로 표시.
- **최대 5개 다중 요금제/회선 탭 지원 (`ProfileTabBar.jsx`)**:
  - 메인 요금제, 스마트폰 테더링, 휴대용 LTE 라우터 등 최대 5개의 요금제 프로필을 등록하고 탭으로 즉시 전환.
  - 각 프로필별로 남은 용량, 월간 한도(GB), 연결 어댑터(Wi-Fi / USB), 리셋일을 100% 독립적으로 분리하여 실시간 측정 및 관리.
  - 탭 내에서 프로필 추가, 수정, 삭제 기능 지원.
- **다중 프로필 설정 정보 백업 내보내기 & 불러오기 (`SettingsModal.jsx`)**:
  - 등록된 모든 요금제 프로필과 설정값을 JSON 파일로 내보내기 및 1초 복원 기능 추가.
- **콤팩트 미니멀 테마 선택기 (`Header.jsx`)**:
  - 헤더의 가로 공간을 차지하던 드롭다운을 제거하고, 슬림한 테마 미니 팝업 버튼으로 최적화.
- **심해 오로라 마린 글래스 테마 & 림 라이트 (`index.css`)**:
  - 딥 네이비 마린 사파이어 그라데이션, 바다 오로라 블러 펄스 효과, 투명 아쿠아 스카이 테두리 글로우 적용.
- **돌고래 지느러미 유선형 비대칭 곡면 (`MiniGadget.jsx`)**:
  - 미니 가젯 프레임에 부드러운 비대칭 라운딩(`border-radius: 22px 10px 22px 22px`) 적용.

### 버그 수정 및 UI 정리 (Bug Fixes & UI Polish)
- **상단 헤더 중복 보정 버튼 제거**:
  - 활성 요금제 탭 및 메인 쿼터 카드 내부의 연필(수정) 아이콘으로 역할을 일원화하고 헤더 우측의 중복된 [보정] 버튼을 제거하여 상단 바를 깔끔하게 정돈.
- **윈도우 창 모서리 투명 클리핑 (`clip-path`)**:
  - 창 외곽 모서리 바깥쪽에 회색 삼각형 잔상이 남던 문제를 `clip-path: inset(...)` 적용으로 100% 투명화 처리.
- **CSV 리포트 내보내기 엑셀 한글 깨짐 수정**:
  - UTF-8 BOM(`\uFEFF`) 바이트 순서 표식을 추가하여 엑셀에서 열었을 때 프로필명 및 한글 텍스트가 정상 출력되도록 수정.
- **사용자 보정 데이터 영구 보존 연속성 복구**:
  - 윈도우 OS의 WebView2 저장소 식별자(`identifier`)를 통일하여 버전 업데이트 후에도 기존에 입력해 둔 요금제명, 한도(GB), 사용량 보정 숫자가 안전하게 유지되도록 조치.
- **미니 가젯 수정 모달 뷰포트 최적화**:
  - 미니 가젯(320x150)에서 연필(수정) 아이콘 클릭 시, 윈도우 창이 원래 대시보드 크기(`1120x760`)로 복원되며 사용량 보정 모달이 화면 중앙에 열리도록 UX 개선.

---

## [1.1.0] - 2026-08-11

### 주요 추가 기능
- **공식 마스코트 아이콘 확정 (젠틀 네이비 돌고래)**:
  - 뽀얀 흰색 배를 보존하고 파스텔 스카이블루 Wi-Fi 아치가 어우러진 고가독성 볼드 아이콘 적용 (icon.ico, 트레이, 작업표시줄 일괄 반영).
- **실시간 프로세스 트래픽 추적 & 프로세스 종료 기능 (`AppBreakdownCard.jsx`)**:
  - PC에서 데이터를 많이 쓰는 프로그램(PID)과 연결 도메인을 실시간 추적하고, 특정 앱을 원클릭으로 종료하여 데이터 소진을 멈추는 기능 탑재.
- **1줄 스마트 요금제 비교 바 (`QuotaRingCard.jsx`)**:
  - 실시간 남은 데이터 사용량에 따라 알뜰폰 요금제를 추천하는 1줄 콤팩트 바 연동.
- **네이티브 윈도우 창 리사이징 (`set_mini_mode`)**:
  - 미니 가젯 전환 시 윈도우 창 자체가 화면 구석에 컴팩트한 320px x 150px 크기로 축소되며 항상 위(Always-on-top) 고정.

### 버그 수정
- **실시간 속도계 멈춤 현상 수정**:
  - Rust 백엔드의 sysinfo 0.30 네트워크 증분 델타 계산 로직을 바로잡아 실시간 속도계 정상화.
- **타이틀바 버튼 클릭 이벤트 분리**:
  - 드래그 영역과 윈도우 컨트롤 버튼(-, □, x)의 이벤트 버블링 분리.

---

## [1.0.0] - 2026-08-07

### 최초 릴리즈 (Initial Release)
- 실시간 LTE 라우터 & Wi-Fi 데이터 사용량 모니터링 데스크톱 앱 출시.
- 60초 롤링 타임시리즈 트래픽 그래프 & 순간 속도계 게이지.
- 매월 자동 0 GB 초기화 및 통신사 사용량 보정 모달.
- 윈도우 시스템 트레이 최소화 & 80%, 90%, 95% 임계치 윈도우 푸시 알림.
- 미니 플로팅 가젯 모드 및 4종 테마 지원.
